<form action="/todos" method="POST">
  {{!-- action ,定義表單發送位置 method 用 POST 方法把資料送給 /todos--}}
  <input type="text" placeholder="name" name="name">
  {{!-- <input> 標籤的 name 屬性，這個 name 屬性很重要，它是 HTTP 傳輸時的資料名稱。資料會被放在 HTTP 的 request body 裡，進到 Express.js 以後打包成 req.body 物件
  --}}
  <button type='submit'>Create</button>
</form>